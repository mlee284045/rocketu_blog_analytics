import os
import appenlight_client.client as e_client
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql_psycopg2',
        'NAME': '{{ db_name }}',
        'HOST': 'localhost',
        'USER': '{{ db_user }}',
        'PASSWORD': '{{ db_password }}'
    }
}

BASE_DIR = os.path.dirname(os.path.dirname(__file__))
STATIC_ROOT = os.path.join(BASE_DIR, 'static')
STATIC_URL = '/static/'

DEBUG = False
ALLOWED_HOSTS = ['{{ nginx_server_name }}']

RAVEN_CONFIG = {
    'dsn': 'https://a9f626201bc34190b55696477a4ecbec:57149106aa6c480d905d77a5327858be@app.getsentry.com/32056',
}

APPENLIGHT = e_client.get_config({'appenlight.api_key':{{ appenlight_key }})